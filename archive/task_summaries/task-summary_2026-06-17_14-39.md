# Task Summary: Architecture Reconnaissance & Audit

## Objective
Read through the entire 太初五岳 codebase to separate marketing language from actual code behavior, and produce an honest architectural map.

## Key Actions
1. Read all 14 module source files across two directories:
   - `/Users/apple/Desktop/太初五岳开源/` (open-source repo)
   - `/Users/apple/太初私库/SOMA_X/` (private brain system)
2. Read the llama.cpp integration (`dalin_soma.cpp`)
3. Cross-reference code with patents, papers, and previous audit findings

## Key Findings

### Three Independent Code Layers (No Code Sharing)
1. **SOMA X Brain** — Concept prototype using predictive coding + surprise detection (NOT attention)
2. **Open-Source Five Mountains** — MLX prototypes that actually implement SFA concepts
3. **llama.cpp Integration** — Architecture skeleton, SFA core not yet implemented

### What's Real vs Not
| Module | Real Implementation? | Evidence |
|--------|---------------------|----------|
| SFA Engine | ✅ Yes | RingBuffer + EMA field state + dual-channel attention |
| LingYa PEFT | ✅ Yes | `W = I + R@P·α`, orthonormal scaffold + zero-init growth |
| Guiyuan v2 | ✅ Yes | Three-channel fusion: anchor KV + signal field EMA + Gaussian decay |
| Heritage Distillation | ✅ Yes | Learnable compression queries + three-layer loss |
| SFA in llama.cpp | ❌ Skeleton only | Uses standard `build_attn()`, no RingBuffer, no EMA |
| SOMA X SignalFieldAttention | ❌ Different | Predictive coding + surprise detection, not attention |

### 5 Bugs Found
1. `soma_engine.py`: `decode_step()` creates new RingBuffer instead of in-place update
2. `soma_engine.py`: First token outputs zero (field_state is all zeros)
3. `03_soma_native/源代码.py`: `out_weight[0]` bug — only uses first weight row
4. Code duplication between `soma_engine.py` and `04_soma_convergence/源代码.py`
5. `llama.cpp`: No true SFA ring buffer or EMA state

### Data Gap
- Paper claims (Cosine Similarity > 0.9999999, 248× compression, 4.16× speedup) have **no measured code**
- All claims are theoretical targets or simulation results
- Need real-world benchmarking on actual models

## Artifact
- `/Users/apple/Desktop/太初五岳开源/ARCHITECTURE_RECONSTRUCTION_REPORT.md` — Full 8.6KB audit report with all findings

## Next Steps
1. Fix bugs in `03_soma_native/源代码.py` and `soma_engine.py`
2. Run `soma_engine.py` `__main__` block to get actual cosine similarity data
3. Implement true SFA in llama.cpp KV cache layer
4. Benchmark on real Llama-3-8B model to get measured data
